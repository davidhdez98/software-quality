language: node_js
node_js:
  - "6"
jdk:
  - oraclejdk8
addons:
  sonarcloud: true
script:
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=davidhdez98-github -Dsonar.projectKey=software-quality -Dsonar.sources='./src' -X
